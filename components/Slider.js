import React, { Component, PropTypes } from 'react'

class Slider extends Component {
    render() {
        return (
            <div>Slider</div>
        )
    }
}

Slider.propTypes = {}
Slider.defaultProps = {}

export default Slider
